<!DOCTYPE html>
<html lang="ua">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Master Zoo</title>
	<link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body>
	<div class="wrapper">
		<%- include('../../partials/header.ejs') %>
			<main class="page">
				<section class="page__add-product add-product">
					<div class="add-product__container">
						<h2 class="add-product__title">Додати товар</h2>
						<form class="form" action="/products<%= products ? '/' + products.id : '' %>" method="post"
							enctype="multipart/form-data">
							<div class="form__item">
								<label for="article" class="form__label">Артикул:</label>
								<input class="form__input" id="article" type="number" name="article"
									value="<%= products ? products.article : '' %>" required>
							</div>
							<div class="form__item">
								<label for="title" class="form__label">Назва товару:</label>
								<input class="form__input" id="title" type="text" name="title"
									value="<%= products ? products.title : '' %>" required>
							</div>
							<div class="form__item">
								<label for="price" class="form__label">Ціна товару:</label>
								<input class="form__input" id="price" type="number" name="price"
									value="<%= products ? products.price : '' %>" required>
							</div>
							<div class="form__item">
								<label for="description" class="form__label">Опис товару:</label>
								<textarea class="form__textarea" id="description" name="description"
									required><%= products ? products.description : '' %></textarea>
							</div>
							<div class="image-upload" id="uploadArea">
								<span style="<%= products ? 'display: none;' : '' %>">Click or Drag to Upload
									Image</span>
								<img id="previewImage" src="<%= products ? products.imgPath : '' %>"
									alt="<%= products ? products.title : 'Load the image' %>"
									style="<%= products ? 'display: block;' : 'display: none;' %>">
								<input id="fileInput" type="file" name="img" accept="image/*" <%=products ? ''
									: 'required' %> />
							</div>

							<button class="button">
								<%= products ? 'Редагувати товар' : 'Додати товар' %>
							</button>
						</form>
					</div>
				</section>
			</main>
			<%- include('../../partials/footer.ejs') %>
	</div>

	<script src="/javascripts/RequestManager.js"></script>
	<script>
		document.addEventListener('DOMContentLoaded', () => {
			RequestManager.handleFileSelect('#uploadArea', '#previewImage', '#fileInput');
		});
	</script>
</body>

</html>