<!DOCTYPE html>
<html lang="ua">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>
		Master Zoo
	</title>
	<link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
	<div class="wrapper">
		<%- include('../../partials/header.ejs') %>
			<main class="page">
				<div class="page__products products">
					<div class="products__container">
						<h1>Наші продукти</h1>
						<ul class="products__list">
							<% if(productsList.length){ %>
								<% productsList.forEach((product)=> { %>
									<li class="products__item">
										<a href="/products/<%=product.id %>" class="products__link link-products">
											<img src="<%=product.imgPath %>" alt="<%=product.title %>"
												class="link-products__img">
											<div class="link-products__content">
												<div class="link-products__art">
													Артикул: <%= product.article %>
												</div>
												<h3 class="link-products__title">
													<%= product.title %>
												</h3>
												<p class="link-products__price">
													<%= parseFloat(product.price).toFixed(2) %> грн.
												</p>
											</div>
										</a>
									</li>
									<%}) %>
										<%} else {%>
											<li class="products__item">
												Список продуктів порожній
											</li>
											<%} %>

						</ul>
					</div>
				</div>
			</main>
			<%- include('../../partials/footer.ejs') %>
	</div>
</body>

</html>